# Container Diagram — Ponytail

> **C4 Level 2** — agent harness tooling (not a deployed service).

## Diagram

```mermaid
C4Container
    title Container Diagram for Ponytail

    Person(dev, "Developer", "Uses AI coding agents")
    Person(agent, "AI Agent", "Claude Code, Cursor, Codex, etc.")

    System_Boundary(boundary, "Ponytail") {
        Container(rules, "Rules & Skills", "Markdown / MDC", "YAGNI ladder instructions, slash-command skill defs")
        Container(hooks, "Lifecycle Hooks", "Node.js", "SessionStart / prompt injection, mode tracking")
        Container(scripts, "Sync Scripts", "Node.js", "Rule-copy checks, OpenClaw skill builder, benchmarks")
    }

    System_Ext(host, "Agent Host", "Claude Code, Cursor, Codex, OpenCode, Gemini, pi, …")
    System_Ext(clawhub, "ClawHub", "OpenClaw skill registry")

    Rel(dev, host, "Codes with", "IDE / CLI")
    Rel(host, rules, "Loads", "AGENTS.md / .cursor/rules / plugin manifest")
    Rel(host, hooks, "Invokes", "PreToolUse / SessionStart events")
    Rel(scripts, rules, "Validates copies of", "check-rule-copies.js")
    Rel(clawhub, rules, "Distributes", "clawhub install ponytail")
    Rel(agent, host, "Runs inside", "Agent session")
```

> **Note**: this diagram was auto-generated by /handover on 2026-06-19 from repo signals (package.json, hooks/, skills/, agent adapter dirs). It is a **starting point** — review and refine.
>
> - Container labels and tech strings — the detector may have picked a framework version wrong
> - Inferred relationships — user → web assumes HTTPS; adjust if your stack uses something else
> - External systems — anything your team uses that isn't in package.json (e.g. infra-only dependencies, direct cloud APIs called via HTTP) won't have been detected
>
> Update the "Maintenance" section below once the diagram is stable.

## Maintenance

(From the template — update when L2 containers change.)
