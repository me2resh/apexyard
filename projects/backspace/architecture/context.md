# System Context - Backspace

> **C4 Level 1** - the system and everyone/everything it talks to.

## Diagram

```mermaid
C4Context
    title System Context for Backspace

    Person(receptionist, "Receptionist / Cashier", "Checks in visitors, starts sessions, adds charges, checks out visits, records internal payments")
    Person(manager, "Manager / Admin", "Reviews reports, approvals, staff roles, settings, audit log, cash control")
    Person(cleaner, "Cleaner / Maintenance", "Works cleaning queues, maintenance tickets, and space readiness tasks")
    Person(visitor, "Workspace Visitor", "Physical entrant represented by a Visit; does not use Backspace directly in v1")
    Person(host, "Tenant / Host / Event Owner", "Responsible party for hosted guests, events, company billing, or receivables")

    System(main, "Backspace", "Staff-only workspace operations system for Visit-first coworking operations, contextual charges, checkout, internal payments, facilities, and reporting")

    System_Ext(payment_channels, "Manual Payment Channels", "Card terminal, cash drawer, wallet, bank transfer, Instapay; Backspace records internal payment references only")

    Rel(receptionist, main, "Operates daily workflows", "HTTPS")
    Rel(manager, main, "Administers and reviews", "HTTPS")
    Rel(cleaner, main, "Updates facilities work", "HTTPS")
    Rel(visitor, receptionist, "Arrives, requests workspace services", "In person")
    Rel(host, receptionist, "Authorizes guests/events or receives bill", "In person / offline")
    Rel(receptionist, payment_channels, "Takes external/manual payment", "Offline channel")
    Rel(receptionist, main, "Records payment method/reference", "HTTPS")
```

## Scope

Backspace v1 is internal and staff-operated. Visitors, tenants, hosts, and event attendees are domain participants but not direct application users.

## External Relationships

- **Manual Payment Channels** are explicitly not integrated. Backspace records method/reference/outcome only.
- **Better Auth** is an internal package/library integration in this monorepo, not a third-party hosted identity provider for this design.
- No email, SMS, payment provider, analytics, or deployment provider is in scope for the first planning cycle.

---

_Generated by `/c4`-style planning on 2026-06-23. Re-run after architecture changes._
