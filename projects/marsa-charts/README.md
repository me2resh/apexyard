# marsa-charts (project docs)

Per-project docs for the [`marsa-cloud/marsa-charts`](https://github.com/marsa-cloud/marsa-charts) repo — Helm charts for deploying Marsa.

| Doc | What's in it |
|-----|--------------|
| [`handover-assessment.md`](./handover-assessment.md) | First handover read of the repo (state, risks, integration plan, next steps) |
| [`architecture/container.md`](./architecture/container.md) | C4 L2 container diagram — starting-point sketch |
| `docs/agdr/` | Per-project Agent Decision Records |

Registry entry lives in `../../apexyard.projects.yaml` (`name: marsa-charts`).
