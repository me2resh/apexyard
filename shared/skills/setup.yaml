name: setup
description: First-run framework bootstrap — 3 exchanges (describe stack → defaults → accept/customize) and the fork is configured.
effort: medium
body_file: .claude/skills/setup/SKILL.md
